# Consegna dell'esercizio
Scrivi un programma Java che:
- Permetta all’utente di inserire il nome di una città.
- Esegua una richiesta GET a all'API pubblica di OpenMeteo per ottenere le informazioni meteo.
- Stampi temperatura, umidità e condizioni (es. “clear sky”) sulla console.

URL base dell'API di OpenMeteo:
https://api.open-meteo.com/v1/forecast?latitude= + ```latitudine``` + &longitude= + ```longitudine``` + &current_weather=true
![https://api.open-meteo.com/v1/forecast?latitude= + ```latitudine``` + &longitude= + ```longitudine``` + &current_weather=true](kanyeguess.com)
![Screenshot of a comment on a GitHub issue showing an image, added in the Markdown, of an Octocat smiling and raising a tentacle.](https://myoctocat.com/assets/images/base-octocat.svg)
